export type GlobalCommercialConfig={serviceFeeRate:number;serviceFeeMinimum:number;customerFreightMarkupRate:number;fxSpreadRate:number;paymentProcessingRate:number;localDeliveryMarkupRate:number;returnsLossReserveRate:number;minimumContributionMarginRate:number;minimumContributionAmount:number};
export const DEFAULT_GLOBAL_COMMERCIAL_CONFIG:GlobalCommercialConfig={serviceFeeRate:.075,serviceFeeMinimum:5000,customerFreightMarkupRate:.08,fxSpreadRate:.0125,paymentProcessingRate:.018,localDeliveryMarkupRate:.08,returnsLossReserveRate:.012,minimumContributionMarginRate:.04,minimumContributionAmount:5000};
export function commercialAmounts(input:{productCost:number;freightActual:number;localDeliveryActual:number;config?:GlobalCommercialConfig}){const c=input.config??DEFAULT_GLOBAL_COMMERCIAL_CONFIG;const serviceFee=Math.max(c.serviceFeeMinimum,input.productCost*c.serviceFeeRate);const freightMargin=input.freightActual*c.customerFreightMarkupRate;const fxMargin=input.productCost*c.fxSpreadRate;const localDeliveryMargin=input.localDeliveryActual*c.localDeliveryMarkupRate;const paymentProcessing=input.productCost*c.paymentProcessingRate;const returnsReserve=input.productCost*c.returnsLossReserveRate;return {serviceFee,freightMargin,fxMargin,localDeliveryMargin,paymentProcessing,returnsReserve,kareebuRevenue:serviceFee+freightMargin+fxMargin+localDeliveryMargin};}
export function contributionGuard(input:{customerTotal:number;irrecoverableCosts:number;kareebuRevenue:number;paymentProcessing:number;returnsReserve:number;config?:GlobalCommercialConfig}){const c=input.config??DEFAULT_GLOBAL_COMMERCIAL_CONFIG;const contribution=input.customerTotal-input.irrecoverableCosts-input.paymentProcessing-input.returnsReserve;const margin=input.customerTotal?contribution/input.customerTotal:0;return {contribution,margin,passes:contribution>=c.minimumContributionAmount&&margin>=c.minimumContributionMarginRate,requiredContribution:Math.max(c.minimumContributionAmount,input.customerTotal*c.minimumContributionMarginRate)};}
