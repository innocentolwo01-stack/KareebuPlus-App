# Kareebu+ v3.3.1

- Fixes TypeScript compatibility for `react-native-maps` `AnimatedRegion.timing()` with newer React Native typings.
- Keeps the documented latitude/longitude animation payload used by `react-native-maps`.
- No runtime behaviour or native dependencies changed.
